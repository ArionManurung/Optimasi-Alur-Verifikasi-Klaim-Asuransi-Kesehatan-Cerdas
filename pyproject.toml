[project]
name = "tugas01-milestone1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.5",
    "plotly>=7.1.0",
    "streamlit>=1.64.0",
]

[project.scripts]
tugas01-milestone1 = "tugas01_milestone1:main"

[[tool.uv.index]]
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
default = true

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
